## Summary

<!-- What does this PR change and why? Link the ticket: STORY-123 -->

## Checklist

- [ ] Unit/component tests pass (`npm test`)
- [ ] Coverage threshold met (`npm run test:coverage`)
- [ ] Backend tests pass (`cd backend && .venv/bin/python -m pytest`)
- [ ] End-to-end tests pass (`npm run test:e2e`)
- [ ] Lint, formatting, and typechecks pass (`npm run lint`, `npm run format:check`, `npm run build`)
- [ ] OpenAPI spec and backend stay in sync (parity test / `npm run gen:api` output committed if the spec changed)
- [ ] No new secrets, keys, or large binaries committed

## Test plan

<!-- How did you verify the change? List manual or automated checks. -->

## Screenshots (if UI changed)
